import React from 'react';

/*const setActive = ({isActive}: { isActive: boolean }) => isActive ? s.activeLink : s.item*/

export const Header = () => {

    return (
        <div>

        </div>
    );
}
